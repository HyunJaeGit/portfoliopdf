# 작업 지침

- 필요한 범위만 최소 변경하고 단순한 구조를 유지한다.
- 백엔드, API 서버, 데이터베이스, 서버리스 함수를 생성하지 않는다.
- 페이지는 `src/pages/<PageName>` 폴더 단위로 구성한다.
- TypeScript에서 `any`를 사용하지 않는다.
- 미완성 프로젝트인 독학마스터는 어디에도 포함하지 않는다.
- 근거로 확인되지 않은 성과나 본인 기여를 작성하지 않는다.
- 변경 후 `build`와 `lint`를 모두 검증한다.
- GitHub Actions를 이용한 GitHub Pages 정적 배포를 기준으로 한다.
- 작업 결과는 `완료한 작업 / 확인 결과 / 남은 작업` 순서로 보고한다.
